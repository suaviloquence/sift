{
  "name": "sift",
  "version": "1.0.0",
  "description": "",
  "repository": "https://github.com/suaviloquence/sift",
  "author": "m",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "(cd shared && yarn dev) & (cd app && yarn dev) & (cd server && yarn dev)",
    "build": "(cd shared && yarn build) & (cd app && yarn build) & (cd server && yarn build)"
  },
  "devDependencies": {
    "eslint": "^8.12.0"
  }
}
